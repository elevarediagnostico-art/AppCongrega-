import { trpc } from "@/lib/trpc";
import { Award, HeartPulse } from "lucide-react";

export function ExperienceOverview({ churchId }: { churchId: number }) {
  const overview = trpc.experience.overview.useQuery({ churchId }); if (overview.isLoading) return null; const data = overview.data; if (!data) return null;
  return <section className="mt-7 grid gap-5 lg:grid-cols-[0.85fr_1.15fr]"><article className="rounded-3xl bg-[#123d36] p-6 text-[#f8f1df]"><HeartPulse className="size-5 text-[#e8bd68]" /><p className="mt-5 text-xs font-semibold uppercase tracking-[0.16em] text-[#e8bd68]">Índice de Experiência da Igreja</p><p className="mt-2 font-serif text-5xl font-semibold">{data.index}<span className="ml-1 text-base text-white/60">/100</span></p><p className="mt-4 text-sm leading-6 text-white/70">Indicador de uso e ativação nas últimas quatro semanas. Não mede vendas nem indicações.</p></article><article className="rounded-3xl border border-stone-200 bg-white p-6"><Award className="size-5 text-[#b78535]" /><h2 className="mt-4 font-serif text-2xl font-semibold">Experiência real</h2><div className="mt-5 grid gap-3 sm:grid-cols-3"><div><p className="text-xs font-semibold uppercase tracking-[0.12em] text-stone-400">Ativação</p><p className="mt-1 font-serif text-2xl font-semibold">{data.components.activeMembers}</p><p className="text-xs text-stone-500">membros ativos</p></div><div><p className="text-xs font-semibold uppercase tracking-[0.12em] text-stone-400">Uso</p><p className="mt-1 font-serif text-2xl font-semibold">{data.components.activityEvents}</p><p className="text-xs text-stone-500">ações registadas</p></div><div><p className="text-xs font-semibold uppercase tracking-[0.12em] text-stone-400">Liderança</p><p className="mt-1 font-serif text-2xl font-semibold">{data.components.leadershipParticipants}</p><p className="text-xs text-stone-500">participantes</p></div></div>{data.recognitions.length > 0 && <p className="mt-5 border-t border-stone-100 pt-4 text-sm text-[#417468]">Reconhecimentos ativos: {data.recognitions.map(item => item.title).join(" · ")}</p>}</article></section>;
}
